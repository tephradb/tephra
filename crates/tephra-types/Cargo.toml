[package]
name = "tephra-types"
description = "Shared vocabulary types for tephra: positions, event type/tag names, and the query model. Pure data and validation, no I/O."
edition = "2024"
license = "Apache-2.0"
version = "0.1.3"
rust-version = "1.85.0"
repository.workspace = true

[lib]
name = "tephra_types"
path = "src/lib.rs"

[dependencies]
smallvec.workspace = true
thiserror.workspace = true

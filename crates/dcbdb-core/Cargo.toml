[package]
name = "dcbdb-core"
description = "Shared vocabulary types for dcbdb: positions, event type/tag names, and the query model. Pure data and validation, no I/O."
edition = "2024"
license = "Apache-2.0"
version = "0.1.0"

[lib]
name = "dcbdb_core"
path = "src/lib.rs"

[dependencies]
smallvec.workspace = true
thiserror.workspace = true

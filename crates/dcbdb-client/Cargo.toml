[package]
name = "dcbdb-client"
description = "Synchronous, blocking TCP client for a dcbdb event store, speaking the length-prefixed protobuf protocol"
edition = "2024"
license = "Apache-2.0"
version = "0.1.0"

[dependencies]
dcbdb-core.workspace = true
dcbdb-proto.workspace = true
